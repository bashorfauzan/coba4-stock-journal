import React, { useState } from 'react';
import { 
  CheckCircle, 
  Circle, 
  ChevronRight, 
  BookOpen, 
  FileText, 
  Users, 
  MessageSquare, 
  Gavel, 
  Search, 
  GraduationCap,
  Info,
  ExternalLink
} from 'lucide-react';

const App = () => {
  const [activeStep, setActiveStep] = useState(0);

  const steps = [
    {
      title: "Tahap Pra-Syarat",
      icon: <CheckCircle className="w-6 h-6" />,
      description: "Pengecekan otomatis data akademik oleh sistem.",
      details: [
        "Login menggunakan NIM & Password Info MHS.",
        "Status Mahasiswa harus Aktif (A).",
        "Minimal menempuh 100 SKS.",
        "Lulus mata kuliah prasyarat (Metpen/Pra-Skripsi).",
        "IPK sesuai standar minimal prodi."
      ],
      color: "bg-blue-500"
    },
    {
      title: "Pengajuan Judul",
      icon: <FileText className="w-6 h-6" />,
      description: "Langkah awal memulai TA secara administratif.",
      details: [
        "Input judul, abstrak, dan bidang minat.",
        "Upload draf proposal (Bab 1-3) format PDF.",
        "Verifikasi oleh Admin atau Kaprodi.",
        "Status: Diterima, Direvisi, atau Ditolak."
      ],
      color: "bg-indigo-500"
    },
    {
      title: "SK Pembimbing",
      icon: <Users className="w-6 h-6" />,
      description: "Penentuan dosen pendamping penelitian.",
      details: [
        "Plotting Dosen Pembimbing I & II oleh Kaprodi.",
        "Penerbitan SK Digital oleh sistem.",
        "Download SK dari dashboard untuk arsip.",
        "Mahasiswa resmi memulai masa bimbingan."
      ],
      color: "bg-purple-500"
    },
    {
      title: "Bimbingan & Logbook",
      icon: <MessageSquare className="w-6 h-6" />,
      description: "Proses konsultasi dan pengerjaan naskah.",
      details: [
        "Upload progres Bab 1-5 secara bertahap.",
        "Gunakan link Google Drive (Akses: Public Viewer).",
        "Wajib mengisi Logbook Digital setiap konsultasi.",
        "Mendapatkan status ACC Sidang dari semua pembimbing."
      ],
      color: "bg-pink-500"
    },
    {
      title: "Sidang Tugas Akhir",
      icon: <Gavel className="w-6 h-6" />,
      description: "Ujian lisan di depan dewan penguji.",
      details: [
        "Upload syarat sidang (TOEFL, Bebas Keuangan, dll).",
        "Penentuan Dosen Penguji oleh sistem.",
        "Menerima jadwal dan lokasi sidang di dashboard.",
        "Pelaksanaan presentasi dan tanya jawab."
      ],
      color: "bg-orange-500"
    },
    {
      title: "Pasca-Sidang",
      icon: <Search className="w-6 h-6" />,
      description: "Revisi dan pengecekan kualitas karya.",
      details: [
        "Input nilai oleh Dosen Penguji ke SAMANTA.",
        "Upload bukti perbaikan/revisi naskah.",
        "Uji Turnitin (Plagiasi) oleh Admin Prodi.",
        "Batas maksimal plagiasi (biasanya < 25%)."
      ],
      color: "bg-teal-500"
    },
    {
      title: "Yudisium & Lulus",
      icon: <GraduationCap className="w-6 h-6" />,
      description: "Pernyataan kelulusan dan persiapan wisuda.",
      details: [
        "Lengkapi biodata ijazah & upload foto resmi.",
        "Validasi akhir oleh Biro Akademik.",
        "Status berubah menjadi 'Lulus'.",
        "Data dikirim ke PDDIKTI Nasional."
      ],
      color: "bg-green-600"
    }
  ];

  return (
    <div className="min-h-screen bg-slate-50 p-4 md:p-8 font-sans text-slate-900">
      <div className="max-w-5xl mx-auto">
        {/* Header */}
        <div className="mb-10 text-center">
          <h1 className="text-3xl md:text-4xl font-bold text-slate-800 mb-2">
            Peta Jalan Digital <span className="text-blue-600">SAMANTA</span>
          </h1>
          <p className="text-slate-600">Panduan Interaktif Alur Manajemen Tugas Akhir</p>
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-12 gap-8">
          {/* Sidebar Navigation (Stepper) */}
          <div className="lg:col-span-4 space-y-2">
            {steps.map((step, index) => (
              <button
                key={index}
                onClick={() => setActiveStep(index)}
                className={`w-full flex items-center p-4 rounded-xl transition-all duration-200 border-2 ${
                  activeStep === index 
                  ? 'bg-white border-blue-500 shadow-md translate-x-2' 
                  : 'bg-transparent border-transparent hover:bg-white hover:border-slate-200'
                }`}
              >
                <div className={`flex-shrink-0 w-10 h-10 rounded-full flex items-center justify-center mr-4 ${
                  activeStep >= index ? step.color + ' text-white' : 'bg-slate-200 text-slate-500'
                }`}>
                  {activeStep > index ? <CheckCircle className="w-6 h-6" /> : (index + 1)}
                </div>
                <div className="text-left">
                  <p className={`font-bold ${activeStep === index ? 'text-blue-600' : 'text-slate-700'}`}>
                    {step.title}
                  </p>
                  <p className="text-xs text-slate-500 truncate w-40">{step.description}</p>
                </div>
                {activeStep === index && <ChevronRight className="ml-auto w-5 h-5 text-blue-500" />}
              </button>
            ))}
          </div>

          {/* Content Detail Area */}
          <div className="lg:col-span-8">
            <div className="bg-white rounded-2xl shadow-xl border border-slate-100 overflow-hidden min-h-[500px] flex flex-col">
              {/* Step Header */}
              <div className={`${steps[activeStep].color} p-8 text-white`}>
                <div className="flex items-center gap-4 mb-4">
                  <div className="p-3 bg-white/20 rounded-lg">
                    {steps[activeStep].icon}
                  </div>
                  <span className="text-sm font-semibold uppercase tracking-wider opacity-80">
                    Langkah {activeStep + 1} dari {steps.length}
                  </span>
                </div>
                <h2 className="text-3xl font-bold">{steps[activeStep].title}</h2>
                <p className="mt-2 text-white/90 text-lg">{steps[activeStep].description}</p>
              </div>

              {/* Step Body */}
              <div className="p-8 flex-grow">
                <h3 className="text-sm font-bold text-slate-400 uppercase tracking-widest mb-6 flex items-center">
                  <Info className="w-4 h-4 mr-2" /> Detail & Persyaratan
                </h3>
                
                <ul className="space-y-4">
                  {steps[activeStep].details.map((detail, idx) => (
                    <li key={idx} className="flex items-start">
                      <div className="mt-1 mr-3 flex-shrink-0">
                        <div className={`w-2 h-2 rounded-full mt-2 ${steps[activeStep].color}`}></div>
                      </div>
                      <span className="text-slate-700 text-lg leading-relaxed">{detail}</span>
                    </li>
                  ))}
                </ul>

                {/* Integration Tips */}
                <div className="mt-10 p-6 bg-slate-50 rounded-xl border border-dashed border-slate-300">
                  <h4 className="font-bold text-slate-800 flex items-center mb-2">
                    <BookOpen className="w-5 h-5 mr-2 text-blue-500" /> Tips Keberhasilan
                  </h4>
                  <p className="text-slate-600 text-sm italic">
                    {activeStep === 3 ? "Pastikan link Google Drive Anda tidak terkunci (Restricted) agar dosen tidak kesulitan saat memeriksa progres." : 
                     activeStep === 5 ? "Simpan bukti Turnitin Anda sebagai arsip pribadi sebelum mendaftar yudisium." :
                     "Selalu periksa menu notifikasi di SAMANTA setiap hari untuk update status terbaru."}
                  </p>
                </div>
              </div>

              {/* Navigation Buttons */}
              <div className="p-6 border-t border-slate-100 bg-slate-50/50 flex justify-between items-center">
                <button 
                  onClick={() => setActiveStep(prev => Math.max(0, prev - 1))}
                  disabled={activeStep === 0}
                  className="px-6 py-2 font-semibold text-slate-500 hover:text-slate-800 disabled:opacity-30 transition-colors"
                >
                  Sebelumnya
                </button>
                <div className="flex gap-1">
                  {steps.map((_, i) => (
                    <div key={i} className={`w-2 h-2 rounded-full ${activeStep === i ? 'bg-blue-500 w-4' : 'bg-slate-300'}`}></div>
                  ))}
                </div>
                <button 
                  onClick={() => setActiveStep(prev => Math.min(steps.length - 1, prev + 1))}
                  className={`px-8 py-3 rounded-full font-bold text-white shadow-lg transition-transform active:scale-95 ${
                    activeStep === steps.length - 1 ? 'bg-green-600' : 'bg-blue-600 hover:bg-blue-700'
                  }`}
                >
                  {activeStep === steps.length - 1 ? 'Selesai!' : 'Lanjut'}
                </button>
              </div>
            </div>
            
            <div className="mt-6 flex items-center justify-center gap-6 text-slate-400 text-sm">
               <div className="flex items-center"><ExternalLink className="w-4 h-4 mr-1"/> samanta.umm.ac.id</div>
               <div className="flex items-center"><Circle className="w-3 h-3 fill-green-500 text-green-500 mr-1"/> Sistem Online</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default App;


       v    vvvvvvvvvaaaaauupo]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]jjjjjjjjjjjiˆº